# For local testing only — GitHub Actions uses repo secrets
   # Copy to .env and fill in, then: export $(cat .env | xargs) && python run.py
   TWELVE_DATA_API_KEY=your_key_here
   FRED_API_KEY=your_key_here
